package com.pawhub.domain;

/** How this cat tends to act — used for PawMatch reciprocal “behavior” filters. */
public enum CatBehavior {
    PLAYFUL,
    CALM,
    CUDDLY,
    INDEPENDENT,
    CURIOUS,
    CHILL
}
