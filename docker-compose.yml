services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: pawhub
    ports:
      - "3306:3306"
    volumes:
      - pawhub_mysql:/var/lib/mysql

volumes:
  pawhub_mysql:
