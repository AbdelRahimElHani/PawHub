# Backend public origin (no trailing slash). Required for production build so API + WebSocket URLs are correct.
# Example: VITE_API_BASE_URL=https://your-pawhub-api.up.railway.app
# Leave unset for local dev (uses Vite proxy to localhost:8080).
# VITE_API_BASE_URL=
